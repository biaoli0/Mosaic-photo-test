# HTTP server bind address and port (`src/index.ts`).
PORT=3001
HOST=0.0.0.0

# Max raw image upload size (bytes). Default shown below is 20 MiB.
MAX_BODY_BYTES=20971520

# Allowed range for the `tileSize` query parameter on POST /mosaic.
MIN_TILE_SIZE=2
MAX_TILE_SIZE=256
