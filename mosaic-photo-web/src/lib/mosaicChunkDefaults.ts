/** Baseline chunking tuned in `+page.server.ts` via private env. */
export const DEFAULT_CHUNK_TARGET_HEIGHT = 1024;
export const DEFAULT_CHUNK_CONCURRENCY = 4;

/** Tile size slider bounds / default (`+page.server.ts`, +page.svelte). */
export const DEFAULT_TILE_SLIDER_MIN = 2;
export const DEFAULT_TILE_SLIDER_MAX = 64;
export const DEFAULT_TILE_SIZE = 12;
